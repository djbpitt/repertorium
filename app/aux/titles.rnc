start = titles
titles = element titles { title+ }
title = element title { bg, en, ru, bibl? }
bg = element bg { xsd:string { pattern = '[^\n]+' } }
en = element en { xsd:string { pattern = '[^\n]+' } }
ru = element ru { xsd:string { pattern = '[^\n]+' } }
bibl = element bibl { text }
